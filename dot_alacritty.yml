shell:
  program: fish
  args:
    - --login 
    - -c 
    - tmux
    - task

import:
  - ~/.config/alacritty/themes/themes/everforest_dark.yaml

window:
   dimensions:
     columns: 150
     lines: 50


env: 
  WINIT_X11_SCALE_FACTOR: "1"

font:
  size: 12


